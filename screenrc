# no visual bell
vbell off
zombie cr
bind B break 3
ignorecase on
startup_message off
shell -/bin/bash
defscrollback 1000

# keys
bind j select

hardstatus alwayslastline
hardstatus string '%{= kG}[ %{G}%H %{g}][%= %{= kw}%?%-Lw%?%{r}(%{W}%n*%f%t%?(%u)%?%{r})%{w}%?%+Lw%?%?%= %{g}][%{B} %m-%d %{W}%c %{g}]'
